export const focusTo = (index) => {
	// console.log("🚀 ~ file: textAreaFocus.js:2 ~ focusTo ~ index:", index)
	// const toBeFocused = document.getElementById(`todoText${index}`);
	// if (toBeFocused) {
	//     const toBeFocused = document.getElementById(`todoText${index}`);
	// 	console.log('focusing to:', toBeFocused);
	// 	toBeFocused.focus();
	// }
	index.focus();
};
